/*
function power (num, degree) {
    let res = 1;
    for (let i=1; i <= degree; i++) {
        res *= num;
    }
    return res;
}
*/

/*
function random (min, max) {
    let randomNum = Math.random() * (max-min) + min;
    return Math.trunc(randomNum);
}

alert(random(+prompt("",6),+prompt("",10)));
*/

/*
Функция принимает длинну и ширину комнаты
Вычисляет и возвращает площадь комнаты
*/

/*
function square (width, length) {
    return width * length;
}

alert(square(6,5));
*/

/*
Функция получает рост (см) и вес (кг)
Вычесляет индекс массы тела человека
160cm
60kg
*/

/*
function massIndex (hight, weidth) {
    hight /= 100;
    return weidth / hight**2;
}

alert(massIndex(160,60));
*/